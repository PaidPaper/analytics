name: Test Checks 2

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Simple test
        run: echo "Running tests..."
      
      - name: Wait (to see in-progress status)
        run: sleep 15
      
      - name: Success
        run: exit 1
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     if: |
#       github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'run-test')
#     steps:
#       - name: Wait 2 seconds
#         run: sleep 2
      
#       - name: Run test
#         run: echo "✅ Test check completed"
#       # - uses: actions/checkout@v4
      
      # - name: Simple test
      #   run: echo "Running tests..."
      
      # - name: Wait (to see in-progress status)
      #   run: sleep 10
      
      # - name: Success
      #   run: exit 1
      
